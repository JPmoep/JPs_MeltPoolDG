SET(TEST_LIBRARIES meltpooldg)
SET(TEST_TARGET meltpooldg)
DEAL_II_PICKUP_TESTS()

SET(_n_tests "0")
FILE(GLOB _tests ${_testexe}*.json)
LIST (SORT _tests)
FOREACH(_test ${_tests})
  MATH(EXPR _n_tests "${_n_tests} + 1")
ENDFOREACH()
MESSAGE(STATUS "Added ${_n_tests} tests")
